services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      - POSTGRES_USER=dev
      - POSTGRES_PASSWORD=dev
    volumes:
      - postgresql-data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
volumes:
  postgresql-data:
